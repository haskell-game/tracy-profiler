packages: .

package tracy-profiler
  flags: +enable +manual_lifetime

-- XXX: cabal doesn't handle relative paths
-- extra-include-dirs: ./upstream/tracy/public/tracy
-- extra-lib-dirs: ./upstream/_build/library
